# mini-coder 前置知识学习路线

> 学习原则：**先动手体验到，再理解原理**。每个模块 = 概念 + 亲手实验 + 检验标准。
> 检验标准只有一个：**不看资料，能把这件事讲清楚**（费曼式）。讲给我听，通过才进下一模块。
> 所有实验放 `learning/` 目录，与正式代码隔离；博客素材随手记进 `NOTES.md`。

---

## 总览：一切知识都挂在这张图上

Agent 的本质核心其实只有十几行伪代码，全部前置知识都是为了看懂它、讲透它：

```
messages = [系统提示 + 用户输入]
loop:
    响应 = LLM(messages)          # 模型只能"说"，不能"做"
    if 响应要求调用工具:
        结果 = 本地执行那个工具     # 程序负责"做"
        messages.append(响应, 结果)
    else:
        输出最终回复，退出循环
```

后面每个模块都是在回答这张图里的一个问题：

| 模块 | 回答的问题 |
|------|-----------|
| M1 LLM API 本质 | `LLM(messages)` 到底是什么？messages 是什么？ |
| M2 工具调用机制 | "响应要求调用工具"是怎么表达的？怎么执行、怎么回传？ |
| M3 Agent 循环 | 循环什么时候停？怎么让模型自主决定？ |
| M4 System Prompt | 怎么让同一个模型表现得像个"编程助手"？ |
| M5 上下文与工程 | 历史越滚越长怎么办？出错、越权怎么办？ |
| M6 生态与调研 | Claude Code 还有什么机制？别人的实现什么样？ |

---

## M1：LLM API 的本质 —— 无状态的文本进、文本出

**学习目标**
- 理解 `messages` 数组结构：`role`（system / user / assistant）与 `content`
- 理解"无状态"：模型不记得上次对话，多轮 = 客户端每次重发全部历史
- 理解 token 与上下文窗口：计费按 token，历史越滚越长意味着什么
- 认识流式输出（SSE）：为什么所有 AI 客户端都是"打字机效果"

**动手实验**
1. 用 curl 直接调一次 Anthropic（或 OpenAI 兼容）API，**不用任何 SDK**，观察请求体和响应体的原始 JSON 结构
2. 写一个 ~15 行的脚本，手动做两轮对话：第二轮把第一轮的问答都拼进 messages 再发——亲身体会"记忆是重发出来的"
3. 加 `stream: true`，观察服务端推回来的数据块长什么样

**检验问题**
- 为什么第二次调用必须带上第一次的回复？
- 上下文窗口满了会发生什么？Claude Code 是怎么应对的（凭你日常使用的观察）？
- 流式和非流式，请求体差在哪、响应差在哪？

---

## M2：工具调用（Function Calling）机制 —— Agent 的手和脚

**学习目标**
- 会用 JSON Schema 定义一个工具（名字、描述、参数）
- 理解完整流程：请求带 tools → 模型返回 tool_use（它想调什么、参数是 JSON）→ **你的程序执行** → 把 tool_result 追加进 messages 再发 → 模型继续
- 认清关键事实：**模型从不执行任何东西，它只是"说出想调用的工具和参数"**，执行权永远在你手里——这是理解一切 Agent 安全问题的根基
- 知道模型为什么会"决定"调用工具（靠工具的 name/description 描述，而不是魔法）

**动手实验**
1. 定义一个 `get_weather(city)` 假工具（不真查天气，返回固定值），跑通"请求 → tool_use → 执行 → tool_result → 最终回答"完整两步请求
2. 故意描述一个模糊的工具，观察模型乱传参数——体会 description 的质量直接决定行为质量
3. 同时给两个工具，看模型怎么选

**检验问题**
- tool_use 和 tool_result 在 messages 里分别是什么 role、什么结构？
- 模型返回的参数 JSON 是可信的吗？如果参数不符合 schema 会怎样？
- 为什么说"执行权在客户端"是权限确认机制能存在的前提？

---

## M3：Agent 循环 —— 把工具调用变成"自主干活"

**学习目标**
- 理解 Agent = while 循环：工具调用不是一次性的，而是反复"想→做→看结果→再想"
- 理解停止条件：模型不再要求调用工具时自然结束；以及强制上限（max iterations）为什么必须有
- 认识 assistant 的"思考"（thinking/CoT）在循环里的位置
- **毕业实验：写一个 ~50 行的裸 Agent**，有 read_file 和 write_file 两个工具，能用自然语言让它在目录里建一个文件——这就是 mini-coder 阶段 0/1 的原型

**检验问题**
- 一个最简 agent 循环的伪代码，不看资料默写
- 为什么要设最大迭代次数？不设会发生什么？
- 模型执行到一半"忘了"之前工具返回的内容，可能是什么原因？

---

## M4：System Prompt 与行为设计 —— 塑造"它是个编程助手"

**学习目标**
- 理解 system prompt 如何改变模型行为：身份、规则、输出格式、工具使用策略
- 观察 Claude Code 的实际行为，反推它的 system prompt 设计（比如：为什么它先读文件再动手？为什么它简短回复？）
- 理解 few-shot：在对话里给示例如何影响行为

**动手实验**
1. 给 M3 的裸 Agent 写一版 system prompt（你是终端编程助手，修改文件前必须先读……），对比行为差异
2. 观察 Claude Code 真实使用（记进 NOTES.md 使用观察栏）

---

## M5：上下文管理与工程健壮性 —— 从 demo 到可用

**学习目标**
- token 计量与上下文窗口策略：截断、摘要压缩（/compact 的原理）、哪些内容优先保留
- 错误处理：API 超时/限流（429/529）的重试与退避
- 安全设计：工具分级（只读/可写/危险）、权限确认的交互模型、Ctrl+C 中断
- 成本意识：一次 Agent 会话的 token 去向（system prompt、历史、工具结果都是消耗）

---

## M6：生态视野与同类调研 —— 收尾进阶段 0

**学习目标**
- 知道 MCP（Model Context Protocol）解决什么问题，和我们的 mini-coder 是什么关系（了解即可，不实现）
- 调研 3~5 个同类"手写 agent"实现，提炼共同模式与缺陷，写进 NOTES.md 调研栏
- 读 Anthropic《Building Effective Agents》一文，对照我们学到的循环：他们说的 workflow 和 agent 区别是什么

---

## 进度

- [x] M1 LLM API 本质（总结见 [learning/M1-学习总结.md](learning/M1-学习总结.md)）
- [x] M2 工具调用机制（总结见 [learning/M2-学习总结.md](learning/M2-学习总结.md)）
- [ ] M3 Agent 循环（毕业实验）
- [ ] M4 System Prompt
- [ ] M5 上下文与工程
- [ ] M6 生态与调研
- [ ] 全部通过 → 启动阶段 0
